# VECTRA — AI NPC 模拟系统 · 项目介绍

> **一句话概括**：用自然语言创建世界、设定居民，然后走进这个世界与 AI 居民实时对话。

---

## 核心概念

**VECTRA** 是一个零依赖的 AI 驱动 NPC 模拟系统。它将「世界创世」与「角色扮演」分离为两个清晰的模式，让你在几分钟内构建一个由 AI 居民组成的活世界。

### 设计哲学

- **零依赖**：纯原生 HTML + CSS + JS，无需 npm/webpack/React
- **双模式**：创世模式（播种者帮你构建）→ 游玩模式（沉浸式对话）
- **双存储**：localStorage 快速启动 / 后端服务器持久化
- **AI 原生**：所有 NPC 对话由 LLM 实时生成，支持 OpenAI / DeepSeek 等

---

## 功能矩阵

| 功能模块 | 描述 | 状态 |
|----------|------|------|
| **创世模式** | 播种者 AI 对话引导，四阶段渐进创世 | ✅ |
| **NPC 双记忆** | 短期记忆（上下文）+ 长期记忆（JSONL 持久化） | ✅ |
| **时钟系统** | 游戏内时间流动，支持 0.5×/1×/2×/5× 倍速 | ✅ |
| **世界事件日志** | 自动记录每一次互动和世界变化 | ✅ |
| **NPC 行为契约** | 说话风格 / 口头禅 / 习惯动作自定义 | ✅ |
| **导演 AI** | 控制 NPC 回复顺序，避免抢话/认错人 | ✅ |
| **记忆档案浏览器** | 按标签筛选、检索、编辑记忆条目 | ✅ |
| **暗色科幻 UI** | 沉浸式界面设计 | ✅ |

---

## 技术架构

```
┌─────────────────────────────────────────────┐
│                  浏览器端                      │
│  ┌─────────┐  ┌──────────┐  ┌─────────────┐ │
│  │ 创世模式 │  │ 游玩模式  │  │  存储适配器  │ │
│  └────┬────┘  └────┬─────┘  └──────┬──────┘ │
│       │            │               │         │
│       └────────────┼───────────────┘         │
│                    ▼                         │
│              app.js (状态机)                  │
│                    │                         │
│         ┌──────────┴──────────┐              │
│         ▼                     ▼              │
│    storage.js           LLM API 调用         │
│         │                     │              │
└─────────┼─────────────────────┼──────────────┘
          │                     │
          ▼                     ▼
   ┌──────────────┐    ┌────────────────┐
   │ localStorage │    │ OpenAI 格式 API │
   │   (默认)      │    │  (DeepSeek等)   │
   └──────────────┘    └────────────────┘
          │
          ▼ (可选)
   ┌──────────────┐
   │  server.py   │
   │  文件系统存储  │
   └──────────────┘
```

---

## 使用场景

1. **游戏原型设计**：快速测试 NPC 对话系统和世界设定
2. **互动小说创作**：让读者与 AI 角色实时互动
3. **角色扮演**：与 AI 展开沉浸式第一人称对话
4. **AI 实验**：测试不同 prompt 对 NPC 行为的影响

---

## 快速体验

### 方式一：零安装（30 秒）

```bash
# 直接打开浏览器
open index.html
```

数据存储在 localStorage，刷新不丢失。

### 方式二：后端持久化（推荐）

```bash
# 启动服务器
python3 server.py

# 访问
open https://localhost:8080
```

数据存储在 `~/VectraData/`，支持多世界管理。

---

## API 配置

支持任何 OpenAI 格式的 API：

| 提供商 | 端点示例 | 模型示例 |
|--------|----------|----------|
| OpenAI | `https://api.openai.com/v1` | `gpt-4o` |
| DeepSeek | `https://api.deepseek.com/v1` | `deepseek-chat` |
| 本地 Ollama | `http://localhost:11434/v1` | `llama3` |

---

## 项目结构

```
Vectra/
├── index.html          # 主页面
├── css/style.css       # 暗色科幻主题
├── js/
│   ├── app.js          # 核心状态机 + LLM 调用
│   ├── storage.js      # 存储适配器
│   └── utils/          # 工具函数
├── server.py           # Python 后端（零依赖）
├── install/            # 一键安装脚本
└── release/            # 发行版打包
```

---

## 扩展方向

- NPC 关系图谱可视化
- 多 NPC 同时在线对话
- 场景地图/网格编辑器
- 更多事件类型支持

---

*VECTRA — 让 AI 居民活起来。*
