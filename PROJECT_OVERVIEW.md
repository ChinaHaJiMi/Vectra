# VECTRA — Headless AI Narrative Engine · 项目介绍

> **一句话**：VECTRA 是一个零依赖的无头 AI 叙事引擎。任何 2D 游戏引擎（Unity、
> Godot、Cocos…）把"发生了什么"上报给它，它负责把游戏世界**整理成能讲故事的
> 大脑** —— 生成叙事摘要、整合信息、维护 NPC 关系图、保存角色记忆、生成剧情
> 文字。

---

## 为什么存在（设计动机）

最初的 VECTRA 是一套带完整 WebUI 的「AI NPC 对话网页」。但当你想**用真正的
游戏引擎做一款全 AI 驱动的 2D 游戏**时，网页那套前端毫无用处 —— 渲染归游戏引擎，
叙事脑力才是缺口。于是 v2 把 VECTRA 从"对话软件"转向：

> **一款可以被任意 2D 游戏引擎当作底层叙事模块的服务。**

它不自带画面，不给渲染、不给物理；它给的是游戏缺的那层：**在漫长游玩里，如何
让 AI 记得发生了什么、关系怎么变、故事该往哪走。**

---

## 核心概念

- **世界 (world)**：一场游戏会话。含设定(bible)、玩家、实体名册、时钟、游标。
- **实体 (entity)**：故事里的可记忆单元 —— 角色、玩家、地点、阵营、物品。
- **事件 (event)**：游戏上报的原始"发生了什么"，只追加，带全局 seq。
- **整合 (integrate)**：把事件凝练成结构化**事实**，写入相关**角色记忆**，并
  更新**关系图边**（好感/信任/熟悉度）。= 信息整合。
- **摘要 (summarize)**：把"已摘要段之后的"事件滚动压缩进一条叙事摘要。= 叙事摘要。
- **关系图 (graph)**：节点=实体，边=a↔b 携带 `affinity/trust/familiarity`。
  = NPC 关系图。
- **生成 (narrate)**：结合当前摘要+记忆+情境，产出旁白或角色对白（可 SSE 流式）。
- **行为决策 (decide)**：NPC 不止说话还会行动 —— 依据角色卡+记忆+关系+场景
  产出结构化意图，动作只能从技能清单 `SKILL_MANIFEST` 选择，由游戏侧执行。

三大叙事能力（叙事摘要 / 信息整合 / NPC 关系图）是地基；在这之上用 `decide`
即可把 NPC 从"对话驱动"推进到"全 AI 行为驱动"。

---

## 技术架构

```
┌─────────────────────────── 你的游戏（Unity / Godot …）──────────────────────────┐
│  渲染 · 物理 · 输入 · 关卡            │  叙事 UI：剧情文字/关系面板/日志           │
└──────────────┬───────────────────────┴──────────────────▲────────────────────┘
               │ HTTP / SSE                              │
┌──────────────▼──────────────────────────────────────────┴────────────────────┐
│                       VECTRA 叙事引擎（Python 零依赖，标准库）                  │
│  ┌─────────────┐   ┌──────────────────────────────────────────────────────┐  │
│  │ server.py   │   │ vectra/store.py（持久化+确定性）  vectra/brain.py(LLM)│  │
│  │ 路由/REST/SSE│──▶│  事件台账  事实库  记忆流   关系图   摘要   生成       │  │
│  └─────────────┘   └───────────────┬──────────────────────────────────────┘  │
│                                    ▼                                          │
│                     ~/VectraData/worlds/<id>/  (JSON / JSONL)                 │
└───────────────────────────────────────────────────────────────────────────────┘
```

**双档智能**：配了 OpenAI 格式 LLM（DeepSeek/OpenAI/Ollama…）→ 实时推理；
没配 Key → 确定性启发式兜底，输出结构与在线完全一致。便于离线开发与测试。

---

## 端到端数据流

```
游戏上报名册  ─▶  POST /worlds/{id}/entities
游戏实时上报  ─▶  POST /worlds/{id}/events        （只追加，打游标）
                   │
                   ▼
              POST /worlds/{id}/integrate   ──▶  事实库 facts.jsonl
                                                 相关角色记忆 memories/<eid>
                                                 关系图边 affinity/trust/familiarity
                   │  (游标 integrated 前移)
                   ▼
              POST /worlds/{id}/summarize   ──▶  summary.json（滚动叙事摘要）
                   │  (游标 summarized 前移)
                   ▼
    游戏侧取用    GET /summary     GET /graph     POST /narrate（剧情文字/对白）
                 POST /decide（NPC 行为意图）→ 游戏执行技能 → 观察回填 /events
```

内存中不存状态，全部落盘；每次请求按游标增量处理，天然适合超长流程。

---

## 目录结构

```
Vectra/
├── server.py                # 无头引擎入口（HTTP + SSE，CORS，可配 token/限流）
├── vectra/
│   ├── __init__.py
│   ├── store.py             # 文件持久化 + 确定性大脑（含离线兜底）
│   └── brain.py             # LLM 编排 + 上下文打包 + OpenAI 兼容调用
├── openapi.yaml             # REST 契约（导入 Postman / 生成客户端）
├── samples/demo_client.py   # 完整调用链参考
├── README.md · PROJECT_OVERVIEW.md · CHANGELOG.md
└── LICENSE                  # MIT
```

---

## 安全与健壮性

- **零第三方依赖**，只需 Python 3.8+ 标准库。
- 所有世界/实体 id 在拼接文件路径前经 **目录穿越净化**。
- 每条记忆/事件独立 JSON 行，坏行自动跳过，不拖垮整体。
- 写操作可选 `X-Vectra-Token` 共享令牌门禁；提供 CORS，方便本地跨源调用。
- LLM 失败自动回退确定性路径，游戏侧不因 AI 断连而崩溃。

---

## 扩展方向

- 事件驱动的关系"软化"：长期无交集边随世界时间衰减。
- 实体/位置的 embedding 语义召回（需外部向量依赖）。
- 导演/多角色对白编排接口（谁该开口）。
- 生成更丰富 prompt 编排：高潮/结局/支线调度。

---

*VECTRA 2 — 给每一个 2D 世界装上一颗会讲故事的 AI 大脑。*
